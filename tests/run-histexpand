{
echo "${CSTART}warning${CEND}: all of these tests will fail if history has not been compiled"
echo "${TAB} into the shell"
echo
} >&2
${THIS_SH} ./histexp.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} histexp.right && rm -f ${BASH_TSTOUT}
