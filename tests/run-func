{
echo "${CSTART}warning${CEND}: if you have exported functions defined in your environment,"
echo "${TAB} they may show up as ‘diff’ output."
echo "${TAB} If so, please do not consider this a test failure"
echo
} >&2
${THIS_SH} ./func.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} func.right && rm -f ${BASH_TSTOUT}
