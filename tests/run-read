{
echo "${CSTART}warning${CEND}: please do not consider output differing only in the amount of"
echo "${TAB} white space to be an error."
echo
} >&2
${THIS_SH} ./read.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} read.right && rm -f ${BASH_TSTOUT}
