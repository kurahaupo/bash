{
echo "${CSTART}warning${CEND}: all of these tests will fail if arrays have not"
echo "${TAB} been compiled into the shell"
echo
echo "${CSTART}warning${CEND}: the BASH_ARGC and BASH_ARGV tests will fail if debugging"
echo "${TAB} support has not been compiled into the shell"
echo
} >&2
${THIS_SH} ./array.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} array.right && rm -f ${BASH_TSTOUT}
