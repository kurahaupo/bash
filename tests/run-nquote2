{
echo "${CSTART}warning${CEND}: several of these tests will fail if arrays have not"
echo "${TAB} been compiled into the shell."
echo
} >&2
${THIS_SH} ./nquote2.tests 2>&1 | grep -v '^expect' > ${BASH_TSTOUT}
diff ${BASH_TSTOUT} nquote2.right && rm -f ${BASH_TSTOUT}
