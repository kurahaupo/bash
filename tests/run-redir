{
echo "${CSTART}warning${CEND}: the text of a system error message may vary between systems and"
echo "${TAB} produce ‘diff’ output."
echo
echo "${CSTART}warning${CEND}: if the text of an error message concerning ‘redir1.*’ not being"
echo "${TAB} found or messages concerning bad file descriptors produce diff"
echo "${TAB} output, please do not consider it a test failure"
echo
} >&2
${THIS_SH} ./redir.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} redir.right && rm -f ${BASH_TSTOUT}
