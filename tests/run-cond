{
echo "${CSTART}warning${CEND}: all of these tests will fail if the conditional command has not"
echo "${TAB} been compiled into the shell"
echo
echo "${CSTART}warning${CEND}: some of these tests will fail if extended pattern matching has not"
echo "${TAB} been compiled into the shell"
echo
echo "${CSTART}warning${CEND}: the text of system error messages may vary between systems, causing"
echo "${TAB} ‘diff’ to produce output; please check for semantic equivalence." >&2
echo
} >&2

${THIS_SH} ./cond.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} cond.right && rm -f ${BASH_TSTOUT}
