{
echo "${CSTART}warning${CEND}: UNIX versions number signals and schedule processes differently."
echo "${TAB} If output differing only in line numbers is produced, please"
echo "${TAB} do not consider this a test failure."
echo
} >&2

${THIS_SH} ./heredoc.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} heredoc.right && rm -f ${BASH_TSTOUT}
