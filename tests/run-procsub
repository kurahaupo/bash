{
echo "${CSTART}warning${CEND}: all of these tests will fail if process substitution has not"
echo "${TAB} been compiled into the shell or if the OS does not provide"
echo "${TAB} FIFOs or /dev/fd. Some tests may fail if the OS does not"
echo "${TAB} provide FIFOs."
echo
} >&2

${THIS_SH} ./procsub.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} procsub.right && rm -f ${BASH_TSTOUT}
