{
echo "${CSTART}warning${CEND}: all of these tests will fail if arrays have not"
echo "${TAB} been compiled into the shell"
echo
} >&2
${THIS_SH} ./assoc.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} assoc.right && rm -f ${BASH_TSTOUT}
