{
echo "${CSTART}warning${CEND}: the text of a system error message may vary between systems and"
echo "${TAB} produce ‘diff’ output."
echo
} >&2
${THIS_SH} ./vredir.tests > ${BASH_TSTOUT} 2>&1
diff ${BASH_TSTOUT} vredir.right && rm -f ${BASH_TSTOUT}
